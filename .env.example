# MySQL (local via docker-compose)
DATABASE_URL="mysql://gestao:gestao@localhost:3306/gestao_empresa"

# Sessao (gere com: openssl rand -base64 32)
AUTH_SECRET="altere-esta-chave-em-producao"

# Dominio publico (usado em links e cookies)
APP_URL="https://gestao.suaempresa.pt"

# GitHub Projects / Issues (opcional)
# Token com escopo: repo, read:project
GITHUB_TOKEN=""
GITHUB_OWNER=""
GITHUB_REPO=""
GITHUB_PROJECT_NUMBER=""
